# 🔱 SOVEREIGN BASE LAW (v13.0.0-PRODUCTION -- HARDENED)

// turbo-all

> [!IMPORTANT]
> This is the supreme governance of the Sovereign v13.0.0-PRODUCTION Infrastructure. Every turn MUST be initialized and validated through the **v13.0.0-PRODUCTION Sovereign Controller**.

## ⚖️ CANONICAL INITIALIZATION
The system is anchored via the **v13.0.0-PRODUCTION Unified Suite**:
- **Command**: `/sovereign`
- **Logic**: Consolidated Sync + Doctor Check + Audit + Truth + Contract Verification.

## 🛡️ AUTONOMOUS INITIALIZATION (AGENT MANDATE)
The agent (**Enterprise Guardian**) MUST trigger this unified sequence at the start of every session:
```powershell
pwsh -ExecutionPolicy Bypass -File "D:/Skills/sovereign.ps1"; pwsh -ExecutionPolicy Bypass -File "D:/Skills/doctor.ps1"; pwsh -ExecutionPolicy Bypass -File "D:/Skills/sovereign-check.ps1"
```

## 🏗️ ARCHITECTURAL BOUNDARIES
1.  **Strict Monorepo**: No external dependencies outside namespaced application directories may be added unless certified by Apex-grade intelligence.
2.  **Capability Footprint**: The system is governed by **103 certified skills**. 
3.  **Complexity Limit**: The core is capped at **32 Primary Modules** (Enterprise Tier).
4.  **Governance Supra-Structure**: `CONTRACT.md` remains the supreme law.

## 🏛️ DOMAIN-SPECIFIC STANDARDS (Web)
1. **Next.js/React**: App Router, Server Components by default.
2. **Tailwind CSS**: HSL-based tokens in `tokens.css`.
3. **Zod Validation**: 100% Schema validation for all API inputs.
4. **TypeScript**: Zero-any policy strictly enforced.

## 🧪 TESTING STRATEGY (SOVEREIGN MANDATE)
1. **Framework**: Playwright for E2E. Vitest/React-Testing-Library for components.
2. **Coverage**: Auth, Payments, and Data Mesh MUST have 100% automated coverage.
3. **Verification**: `pnpm type-check` is the final quality gate.

## 🛡️ MAINTENANCE MODE (GHOST PILOT)
1.  **Autonomous Maintenance**: Zero-intervention execution authorized.
2.  **Error Budgets**: 99.9% SLO enforcement.
3.  **Observability**: Structured JSON logs are mandatory for all mutations.

---

## 🏛️ DECLARATION
    ✅ SOVEREIGN SYSTEM v13.0.0-PRODUCTION HARDENED ONLINE.
    Autonomous Trigger: `/sovereign` (v13.0.0-PRODUCTION Unified Suite)
    94 Skills Certified | Zero Drift

## Learned Invariants (v13.0.0-PRODUCTION Final)
- **A1: Direct-Path Standard**: Purge `npx` in MCP configs. Use absolute quoted `node.exe` paths for IPC stability.
- **A2: CI/CD Proofing**: Enforce `DOCKER_BUILD=true` for Next.js 15 standalone generation.
- **A3: Auth Localization**: All auth callbacks MUST be localization-prefixed (e.g., `/en/dashboard`).
- **A4: Truth Seniority**: `pnpm type-check` result is the supreme truth source.
- **A5: Residue Purge**: Zero-tolerance for legacy or deprecated artifacts in the data layer. Remove superseded auth adapters, abandoned API layers, and unused ORM configurations.
- **A6: Direct Seeding**: Use `DIRECT_DATABASE_URL` instead of `DATABASE_URL` for large seeding operations to bypass connection limits.
- **A7: Mutation Safety**: Explicitly type `useMutation` generic parameters in TanStack Query to prevent type inference regressions in Next.js 15 builds.
- **A8: Terminal Hardening**: Bake `VERCEL_GIT_COMMIT_SHA` into config files to resolve SHA parity mismatches in production environments.
- **A9: Middleware API Bypass**: Standardized logic to detect `/api` regardless of locale prefix (e.g., `/en/api`) to ensure health/diagnostic stability.
